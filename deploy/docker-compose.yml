version: "3"

services:
  api:
    build: .
    ports:
      - "5000:5000"

  postgres:
    image: postgres
    environment:
      POSTGRES_USER: mulenetx
      POSTGRES_PASSWORD: mulenetx
      POSTGRES_DB: mulenetx
    ports:
      - "5432:5432"
